# Local Docker Compose defaults. Replace the JWT value before shared deployment.
THREATLENS_JWT_SECRET=replace-with-a-long-random-secret-before-deployment

# This URL is compiled into the frontend and must be reachable by the browser.
VITE_API_BASE_URL=http://localhost:8000
